#
# ~/.bash/aliases.sh
#

alias watch-sync='watch grep -e Dirty: -e Writeback: /proc/meminfo'
alias eopkg='sudo eopkg'
alias ls='ls --color=auto --group-directories-first -v'
alias ip='ip -c'
alias sl='sl -e'
